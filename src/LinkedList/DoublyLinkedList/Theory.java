package LinkedList.DoublyLinkedList;

public class Theory {
    /*
    ->It consists one extra nodes which points to previous node
    ->it makes easier to implement methods with optimisation
    -->you can move in both the sides
    ->previous of head point to null
    ->next of tail points to null
     */
}
