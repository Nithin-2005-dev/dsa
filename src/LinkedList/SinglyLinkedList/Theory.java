package LinkedList.SinglyLinkedList;

public class Theory {
    /*
    head:it is a reference variable that points to first node
    tail:it is a reference variable that points to last node
    node:it is the entity in a linked list.it contains
    ->value
    ->reference to next node

    In single linked list every not just points to next node only it doesn't have any idea about previous node
     */
    /*
    limitations:
    ->we can't directly access the index.
     */
}
