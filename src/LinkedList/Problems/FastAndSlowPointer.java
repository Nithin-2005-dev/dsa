package LinkedList.Problems;

public class FastAndSlowPointer {
    /*
    ->two points are taken at the head
    ->move one pointer to visit all the nodes
    ->move another pointer to double the first pointer speed
    ->if two pointers meets then there is a cycle in linked list
     */
    /*
    uses:
    ->cycle detection
    ->finding a node in cycle
     */
}
