package FileHandling;

public class Theory {
    /*
    streams->sequence of data->1)byte
     |      |                ->2)character(unicode)
     |      |
     |      V
     V    Abstraction that java provides
     java implements these within class hierarchies in java.io package

     byte streams:
     1)input stream
     2)output stream

     character stream:
     1)reader
     2)writer
     */
    /*
    IO exception:corrupt file,not able to read,file not found,etc

    pre-defined streams:
    -) System.out->standard output stream->console
    -) System.in->standard input stream->keyboard
    -) System.err->standard error stream->console
     */
}
