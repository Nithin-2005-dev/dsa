package Heaps;

public class Theory {
    /*
    why heaps?
    ->you are given an array
    ->you need to find the smallest number in it with constant time complexity.
    #solution
    ->every time you insert an element sort the array
     */
    /*
    ->heap is represented as a complete binary tree.
    ->every node value>=all of its children.

    if root=>i=1
    parent(i)=i/2;
    left(i)=2*i;
    right(i)=2*i+1;
     */
}
