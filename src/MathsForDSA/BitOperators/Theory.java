package MathsForDSA.BitOperators;

public class Theory {
    /*
    Bit-manipulation:-
    operators:
    1)AND (if both the values are true then result will be true)
    ->when you & 1 with any number, digits remain same
    2)OR (if any value is true then entire expression is true)
    3)XOR (if and only if one value is true then the expression is true)
    ->a^1=a' (a compliment)
    ->a^0=a
    4)Not(if a is true it gives false)
    5)left shift operator(shifts the bits to left side)
    ->multiply with 2^n
    6)right shift operator(shifts the bits to right side)
    ->divide with 2^n
     */
}
