package MathsForDSA.NumberSystem;

public class Theory {
    /*
    Number system:-
    1)decimal (0-9)
    2)binary (0-1)
    3)octal (0-7)
    4)hexadecimal (0-15)

    conversions:-
    1) decimal to base b
    ->keep dividing with base,take remainders and write in opposite
    2)base b to decimal
    ->multiply and add the power of base with digits
     */
    /*
    negative of a number in binary form:
    1)take compliment
    2)add 1 to it
     */
    /*
    range formula for n bits:
    -2^(n-1) to 2^(n-1)-1
     */
    /*
    number of digits in base b of number n=int(log of n to the base b)+1
     */
}
